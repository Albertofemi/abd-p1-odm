[project]
name = "envivo-fase1"
version = "0.1.0"
description = "EnVivo · fase 1 · modelos y conexión a la base de datos"
requires-python = ">=3.12"
dependencies = [
    "pymongo>=4.10",
    "geopy>=2.4",
    "geojson>=3.1",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]
